# THN retained TEST lifecycle — local reconciliation

Local A–C preparation adds the dedicated immutable TEST lifecycle workflow and mandatory parser/release suite. Registry bootstrap is an exact five-resource extension of the verified 17-resource shared baseline; seven-pair provisioning remains separate and closed. Existing shared templates/parameters and v1 API fields are preserved. Enable/disable use exact ledger and runtime readbacks; retained state/runtime cannot be removed, and superseded versions require explicit Retain.

The same private registry mediator now supports fixed, sanitized, nonce-bound read-only inspection. Auth's intended GetItem consumer exception is reconciled, and the two verified deployment roles receive only exact-partition GetItem with missing/outside-key denies. A separate DescribeTable deny excepts only the mediator and Hub caller. Human mutation authority is never reused as deployment inspection authority.

The [lifecycle guide](../docs/thn-test-release.md) records real prerequisites, post-QA closure and the distinction between 30-day metadata and automatic cleanup; the [IAM matrix](../docs/thn-test-iam.md) separates caller from CFN effects. No AWS/GitHub write, deployment, account action, schedule activation, data deletion or public QA is implied. Local audit evidence is reported separately, not as a completed operational gate.
